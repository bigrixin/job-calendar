@model MyAbilityFirst.Models.ChatRoomViewModel

@for (int i = Model.Messages.Count - 1; i >= 0; i--)
{
	if (Model.Messages[i].FormatAsSenderMessage)
	{
		@Html.Partial("_SenderMessage", Model.Messages[i])
	}
	else
	{
		@Html.Partial("_ReceiverMessage", Model.Messages[i])
	}
}
